# PLAN

## 이번 사이클 목표

PRD.md에 정의된 MUST 1(객관식 자동 집계, 엑셀+캡처 이미지 인식 포함), MUST 2(주관식 원문 반영) 기능을 구현하여, 응답 엑셀 또는 캡처 이미지 업로드부터 결과보고서 다운로드까지 전체 흐름이 실제로 동작하는 첫 버전을 완성한다.

## 성공 기준

- 응답 엑셀을 업로드하면 별도의 수작업 재구성 단계 없이(0단계), 정해진 형식의 교육평가 결과보고서 Excel 파일이 바로 완성되어 다운로드된다. (PRD 3번 기준)
- 객관식 문항은 무응답을 제외한 집계 결과가, 주관식 문항은 제목과 원문 그대로가 결과보고서에 반영된다. (PRD 5번 MUST 1, 2)
- 캡처 이미지를 업로드한 경우, AI가 인식한 값을 담당자가 화면에서 확인·수정한 뒤에만 결과보고서가 생성된다. (PRD 5번 MUST 1)
- 업로드한 원본 파일(엑셀·이미지)과 처리 중 데이터가 서버에 남지 않는다. (PRD 7번 보안 원칙)

## 작업 목록

1. 엑셀 처리 라이브러리(exceljs) 설치 및 프로젝트에 연결한다. *(완료)*
2. 교육명·교육일자 입력 폼과 응답 엑셀/캡처 이미지 업로드 화면을 만든다. *(완료)*
3. 업로드된 엑셀 파일을 서버에서 파싱해 객관식 문항과 주관식 문항을 구분하는 로직을 만든다. *(완료)*
4. 업로드된 캡처 이미지를 OpenAI Vision API로 인식해 문항·응답 데이터를 추출하는 로직을 만든다. *(완료 — gpt-4o + JSON 스키마 강제 출력)*
5. 객관식 문항별 응답을 집계하는 로직을 만든다 (엑셀·이미지 공통, 무응답 제외, 유효 응답자 수 별도 산출). *(완료)*
6. 주관식 문항의 제목과 응답 원문을 그대로 추출·정리하는 로직을 만든다 (무응답 제외). *(완료)*
7. 업로드 후 인식 결과(응답자 수, 객관식/주관식 문항 수·제목)를 보여주는 확인 화면을 만든다. 캡처 이미지로 업로드한 경우 인식된 값을 직접 수정할 수 있게 한다. *(완료 — 이미지 경로는 응답자 수/문항 제목/등급명/응답수/주관식 응답을 모두 화면에서 직접 수정 가능, 수정 시 평균·비율 자동 재계산)*
8. 확인 화면에 "결과보고서 생성" 버튼을 추가한다. *(완료)*
9. 집계된 객관식 결과와 주관식 원문을 정해진 결과보고서 디자인·형식의 Excel로 만드는 로직을 만든다. *(완료 — 엑셀 경로: 원본 응답 / 01_교육평가 요약(집계표+원형 차트) / 02_주관식 의견 3개 시트. 이미지 경로: 원본 그리드가 없어 01_교육평가 요약 / 02_주관식 의견 2개 시트)*
10. "결과보고서 생성" 버튼을 누르면 결과보고서 Excel 파일이 다운로드되도록 연결한다. *(완료)*
11. 업로드부터 다운로드까지 전체 흐름을 엑셀 경로와 이미지 경로 각각 테스트하고, 문항 수가 다른 경우 등 예외 상황을 확인한다. *(완료 — 엑셀: 실제 CS리더교육과정 응답 파일로 검증. 이미지: 합성 테스트 이미지로 인식→확인 화면 수정→결과보고서 생성까지 검증)*
12. 업로드 처리 후 원본 파일(엑셀·이미지)과 임시 데이터가 서버에 남지 않는지 확인한다 (PRD 7번 보안 원칙 검증). *(완료 — 서버 코드 전체에 파일 저장(writeFile 등) 로직 없음을 확인. 이미지는 base64로 변환해 OpenAI API에 전달 후 메모리에서 즉시 소멸)*
13. `npm run lint`, `npm run build`를 통과시키고 `npm run dev`로 실제 화면을 확인한다. *(완료)*
