# 프로젝트 개요

교육평가 결과보고서 자동생성기 — Google Forms 응답 엑셀을 업로드하면, 정해진 형식의 교육평가 결과보고서(Excel)를 자동 생성해주는 업무지원 앱. 자세한 배경·요구사항은 [PRD.md](PRD.md) 참고.

# 기술 스택 (고정)

- Next.js (화면과 API를 한 프로젝트에서 함께 처리), TypeScript, Tailwind CSS
- 다른 프레임워크·라이브러리로 임의로 바꾸거나 마이그레이션을 제안하지 않는다.
- 배포는 Vercel을 사용한다.

# 범위 (이번 버전에서 만드는 것)

- 교육명·교육일자 입력 및 Google Forms 응답 엑셀 업로드
- 객관식 문항 자동 인식·집계 (무응답 제외, 유효 응답자 수 별도 산출)
- 주관식 문항 제목·응답 원문 그대로 반영
- 업로드 결과(응답자 수, 문항 구성) 확인 화면 제공
- "결과보고서 생성" 버튼을 통한 정해진 형식의 Excel 파일 생성·다운로드

# 비범위 (이번 버전에서 절대 만들지 않는 것)

- 여러 교육(엑셀 여러 개) 일괄 처리
- AI를 이용한 주관식 응답 요약이나 의미 해석
- 여러 사용자 계정/로그인 기능
- 결과보고서 디자인·양식을 사용자가 직접 편집하는 기능
- 생성된 결과보고서를 서버/클라우드에 장기 보관하거나 이력을 검색하는 기능

위 목록에 없는 기능은 사용자와 논의 없이 임의로 추가하지 않는다.

# AI가 반드시 지킬 규칙

**MUST 1. 평가 문항 자동 인식·집계 및 결과보고서 양식 재구성**
- 응답 엑셀에서 평가 대상이 되는 객관식 문항을 식별해 문항별 응답 결과를 집계한다.
- 원본 응답 값을 임의로 수정하거나 제외하지 않는다.
- 무응답은 점수 계산에서 제외하되, 해당 문항의 유효 응답자 수는 별도로 산출한다.
- 문항 수와 내용이 교육마다 달라도 집계 결과는 항상 동일한 결과보고서 디자인·형식으로 배치한다.
- 응답자 수, 객관식 문항 수·제목, 주관식 문항 수·제목을 결과 생성 전에 화면에서 확인할 수 있게 한다.

**MUST 2. 주관식 문항 원문 유지 및 결과보고서 반영**
- 원본 엑셀의 주관식 문항 제목을 결과보고서에 그대로 표시하며, 임의의 고정 항목("좋았던 점" 등)으로 재분류하지 않는다.
- 각 문항 제목 아래에 해당 응답을 그대로 나열한다.
- 응답 내용을 요약하거나 의미를 해석해 재작성하지 않고 원문 그대로 반영한다.
- 무응답은 주관식 응답 목록에서 제외한다.

# 보안·개인정보 원칙

- 이름·사번·연락처 등 개인정보를 수집·보관하는 것을 전제로 설계하지 않는다.
- 업로드한 원본 Excel은 서버에 별도 파일로 저장하지 않고 처리하며, 처리 중 생긴 임시 데이터도 결과보고서 생성 완료 후에는 남기지 않는다.
- 생성된 결과보고서는 사용자 다운로드 목적으로만 만들고, 서버에 누적 보관하지 않는다.
- 외부에서도 접속 가능한 웹 서비스(Vercel 배포)라는 점을 항상 고려한다.
- `.env`의 API 키·토큰 값은 코드나 로그, 커밋에 노출하지 않는다.

# 작업 방식

- 코드를 작성하기 전에 PRD.md의 요구사항과 이 문서의 범위·규칙을 벗어나지 않는지 확인한다.
- 실제 업무 방식을 추측하지 말고, 확인되지 않은 부분은 사용자에게 질문한다.
- 구현 난이도가 높은 기능이 나오면 바로 개발하지 말고, 왜 어려운지 설명한 뒤 더 단순한 방법이 있는지 함께 검토한다.
- 개발자가 아닌 사용자도 이해할 수 있는 표현으로 설명한다.
- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 mymy-app 폴더 안에만 만든다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase를 쓸 상황이 생기면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.

# 작업 절차 (검증 루프)

코드를 바꿀 때는 아래 순서를 통과할 때까지 반복한다.

1. 변경한다.
2. `npm run lint`와 `npm run build`를 실행해 오류가 없는지 확인한다.
3. 화면이 바뀐 작업이면 `npm run dev`로 띄워 실제로 보이는지 확인한다.
4. 문제가 있으면 고치고 다시 1)로 돌아간다.
5. 통과하면 무엇을 왜 바꿨는지 한 줄로 요약한다.

@AGENTS.md
