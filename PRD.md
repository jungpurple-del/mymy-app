# 교육평가 결과보고서 자동생성기 서비스 기획서 (PRD)

- 작성자: 정보라
- 작성일: 2026-09-01

## 1. 추진 배경 (문제 정의)

교육을 운영하는 담당자는 QR 설문으로 수집한 Google Forms 응답 엑셀을 정해진 형식의 교육평가 결과보고서로 정리해야 한다. 그러나 교육마다 원본 응답 엑셀의 문항 수와 내용이 조금씩 달라, 매번 문항을 확인하고 각 문항의 응답 결과를 동일한 결과보고서 양식에 맞게 다시 구성해야 한다. 특히 객관식 문항의 응답 결과를 확인하여 정해진 디자인과 형식으로 반복해서 재구성하는 작업과, 여러 개의 주관식 응답을 읽고 '좋았던 점', '보완이 필요한 점', '추후 듣고 싶은 교육' 등으로 주요 의견을 정리하는 작업에 시간이 많이 소요된다. 또한 담당자가 변경되더라도 결과보고서의 형식과 정리 기준이 일관되게 유지될 필요가 있다.

## 2. 현행 방식과 한계

현재는 다음과 같은 순서로 작업을 진행한다.

1. Google Forms에서 설문 응답을 Excel 파일로 다운로드한다.
2. 객관식 문항별 응답 데이터를 확인하고 문항별 응답 결과를 산출한다.
3. 문항별 결과를 정해진 교육평가 양식에 옮겨 동일한 디자인과 형식으로 재구성한다.
4. 주관식 문항은 전체 응답을 확인하여 '좋았던 점', '보완이 필요한 점', '추후 듣고 싶은 교육' 등의 항목으로 분류·정리한다.
5. 하나의 교육에 대한 결과를 최종 Excel 파일로 완성하여 출력·보관한다.

가장 큰 한계는 교육마다 원본 응답 엑셀의 문항 수와 내용이 달라 매번 문항을 새로 확인하고, 동일한 결과보고서 양식에 맞춰 반복적으로 재구성해야 한다는 점이다. 특히 객관식 결과를 문항별로 산출하여 정해진 디자인으로 재구성하는 과정과, 주관식 응답을 하나씩 확인하여 주요 의견을 분류·정리하는 과정에 반복적인 수작업과 시간이 많이 소요된다.

## 3. 목표와 기대효과 (성공 기준)

- 현재: 원본 응답 엑셀을 받아 결과보고서를 만들기까지, 문항 확인 → 객관식 집계 → 양식에 맞춰 재구성 → 주관식 분류·정리까지 매번 수작업으로 진행해야 한다.
- 목표: 원본 응답 엑셀을 업로드하면, 별도의 수작업 재구성 단계 없이(0단계) 정해진 형식의 교육평가 결과보고서가 바로 완성되어 출력·보관할 수 있는 상태로 만든다.

## 4. 사용자와 이용 흐름

교육 담당자가 아래 순서로 사용한다.

교육명·교육일자 입력 + 응답 엑셀 또는 캡처 이미지 업로드 → 자동 분석 → 인식 결과 확인(응답자 수, 객관식 문항 수·내용, 주관식 문항 수·내용 표시) → "결과보고서 생성" 버튼 클릭 → 결과보고서 Excel 파일 생성/다운로드 → 출력·보관

- 교육명, 교육일자는 담당자가 직접 입력한다.
- 응답자 수는 업로드한 엑셀(또는 이미지)에서 자동으로 산출한다.
- 결과보고서를 생성하기 전, 앱이 정상적으로 인식했는지 확인할 수 있도록 인식 결과(응답자 수/문항 구성)를 화면에 보여준다. 엑셀 원본 형태 그대로를 보여줄 필요는 없다.
- 캡처 이미지를 업로드한 경우, 인식 결과 확인 화면에서 담당자가 값을 직접 수정할 수 있다.
- 이번 버전은 교육 1개(파일 1개) 단위로 처리하며, 여러 교육 일괄 처리는 다루지 않는다.

## 5. 주요 기능 (must / nice로 구분)

**MUST 1. 평가 문항 자동 인식·집계 및 결과보고서 양식 재구성**

- 응답 엑셀에서 평가 대상이 되는 객관식 문항을 식별하여 문항별 응답 결과를 집계한다.
- 원본 응답 값을 임의로 수정하거나 제외하지 않는다.
- 무응답은 점수 계산에서 제외하되, 해당 문항의 유효 응답자 수를 별도로 산출한다.
- 문항 수와 내용이 교육마다 달라도 집계 결과는 동일한 결과보고서 디자인과 형식으로 배치한다.
- 앱이 인식한 응답자 수, 객관식 문항 수 및 문항 제목, 주관식 문항 수 및 문항 제목을 결과 생성 전에 화면에서 확인할 수 있도록 한다.
- 응답 엑셀뿐 아니라, 교육평가 시스템 화면을 캡처한 이미지도 업로드해 문항·응답을 인식할 수 있다.
- 이미지에서 AI가 인식한 값은 원본으로 자동 확정하지 않고, 담당자가 화면에서 확인·수정한 뒤에만 결과보고서 생성에 사용한다. (엑셀로 업로드한 값은 기존처럼 임의 수정 없이 그대로 사용한다)

**MUST 2. 주관식 문항 원문 유지 및 결과보고서 반영**

- 원본 엑셀의 주관식 문항 제목을 결과보고서에 그대로 표시하며, 임의의 고정 항목으로 재분류하지 않는다.
- 각 문항 제목 아래에 해당 문항의 응답을 그대로 나열한다.
- 응답 내용을 요약하거나 의미를 해석하여 재작성하지 않고 원문 그대로 반영한다.
- 무응답은 결과보고서의 주관식 응답 목록에서 제외한다.

**NICE**

- 이번 버전에서는 없음.

## 6. 범위와 비범위

**이번에 만드는 것 (범위)**

- 교육명·교육일자 입력 및 Google Forms 응답 엑셀 업로드
- 교육평가 시스템 화면을 캡처한 이미지 업로드도 입력으로 지원 (AI 인식 후 담당자 확인·수정 필요)
- 객관식 문항 자동 인식·집계 (무응답 제외, 유효 응답자 수 별도 산출)
- 주관식 문항 제목·응답 원문 그대로 반영
- 업로드 결과(응답자 수, 문항 구성) 확인 화면 제공
- "결과보고서 생성" 버튼을 통한 정해진 형식의 Excel 파일 생성·다운로드

**이번에 절대 만들지 않는 것 (비범위)**

- 여러 교육(엑셀 여러 개) 일괄 처리
- AI를 이용한 주관식 응답 요약이나 의미 해석
- 여러 사용자 계정/로그인 기능
- 결과보고서 디자인·양식을 사용자가 직접 편집하는 기능
- 생성된 결과보고서를 서버/클라우드에 장기 보관하거나 이력을 검색하는 기능

## 7. 보안·개인정보 검토

- 개인정보 포함 여부: 교육평가 설문은 기본적으로 익명으로 진행되며, 결과보고서 작성에 이름·사번·연락처 등 개인정보는 필요하지 않다. 다만 향후 사용하는 설문에 개인정보가 포함될 가능성을 배제할 수 없으므로, 앱은 개인정보를 수집·보관하는 것을 전제로 설계하지 않는다.
- 파일 저장 방식: 업로드한 원본 Excel은 서버에 별도 파일로 저장하지 않고 처리한다. 처리 과정에서 발생하는 임시 데이터도 결과보고서 생성이 완료된 후에는 남지 않도록 한다. 생성된 결과보고서는 사용자가 다운로드하기 위한 목적으로만 만들고, 서버에 누적 보관하지 않는다.
- 접속 환경: 병원 내부 업무용으로 사용하지만, 특정 PC 전용이 아니라 Vercel에 배포해 웹으로 접속하는 방식이다. 외부에서도 접속 가능한 서비스라는 점을 고려해, 파일과 설문 응답 데이터가 서버에 불필요하게 남지 않도록 하는 것을 원칙으로 한다.
- 업로드된 이미지도 원본 엑셀과 동일하게 서버에 저장하지 않고 처리 후 즉시 삭제한다.

## 8. 기술 스택

- Next.js: 화면(응답 엑셀/이미지 업로드, 인식 결과 확인, 결과보고서 생성 버튼 등)과 서버 쪽 코드(API, 엑셀 처리 로직)를 한 프로젝트에서 함께 처리한다.
- 고정 이유: mymy-app에 이미 Next.js가 설치되어 있고, 이후 Vercel로 배포할 예정이라 그대로 이어서 사용하기 위함이다.
- OpenAI API(비전): 캡처 이미지에서 문항·응답 값을 인식하는 데 사용한다. `.env`의 `OPENAI_API_KEY`로 인증한다.

## 개발 단위

이번 버전은 4번(사용자와 이용 흐름)과 5번(MUST 1, MUST 2)을 기준으로 아래 단위로 나누어 개발한다.

1. **입력 및 업로드**
   - 교육명, 교육일자 입력 폼
   - Google Forms 응답 엑셀 업로드 기능
   - 교육평가 시스템 화면 캡처 이미지 업로드 기능

2. **문항 자동 인식** (MUST 1)
   - 업로드된 엑셀에서 객관식 문항과 주관식 문항을 구분해 인식
   - 업로드된 캡처 이미지는 AI(OpenAI Vision)로 문항·응답 인식
   - 응답자 수, 문항 수·제목 산출

3. **객관식 집계 및 양식 재구성** (MUST 1)
   - 문항별 응답 결과 집계 (무응답은 집계에서 제외하되 유효 응답자 수는 별도 산출)
   - 집계 결과를 결과보고서 고정 디자인·형식에 배치

4. **주관식 원문 반영** (MUST 2)
   - 주관식 문항 제목과 응답 원문을 결과보고서에 그대로 나열 (요약·재분류 없음, 무응답 제외)

5. **인식 결과 확인 화면**
   - 자동 인식 결과(응답자 수, 문항 구성)를 화면에 표시
   - 캡처 이미지로 업로드한 경우, 인식된 값을 담당자가 직접 수정할 수 있는 편집 기능 제공
   - "결과보고서 생성" 버튼 제공

6. **결과보고서 생성 및 다운로드**
   - 확인 완료 후 정해진 형식의 Excel 파일 생성
   - 사용자 다운로드 목적으로만 생성하고 서버에 저장하지 않음 (7번 보안·개인정보 검토 원칙 반영)
