# 교육평가 결과보고서 자동생성기 설계 문서 (DESIGN)

- 작성자: 정보라
- 작성일: 2026-09-02
- 참고 문서: [PRD.md](PRD.md), [PLAN.md](PLAN.md)

## 1. 화면 구성

**화면 1. 업로드 화면**
- 교육명 입력칸
- 교육일자 입력칸
- 파일 업로드 버튼 1개 (.xlsx 엑셀 또는 .jpg/.png 캡처 이미지 — 확장자로 자동 구분)
- "분석하기" 버튼

**화면 2. 인식 결과 확인 화면**
- 응답자 수
- 객관식 문항 목록 (문항명, 집계 결과 미리보기)
- 주관식 문항 목록 (문항명, 응답 개수)
- 캡처 이미지를 올린 경우: 인식된 값을 직접 고칠 수 있는 입력칸
- "결과보고서 생성" 버튼 → 누르면 결과보고서 Excel 파일이 바로 다운로드됨

## 2. 데이터 흐름

1. 담당자가 화면 1에서 교육명·교육일자를 입력하고 파일을 올린다.
2. 서버로 파일이 전달되고, 파일 종류에 따라 나뉜다.
   - 엑셀(.xlsx) → 셀 내용을 그대로 읽어서 객관식/주관식 문항을 구분
   - 이미지(.jpg/.png) → AI(OpenAI Vision)가 화면 속 글자·숫자를 읽어서 문항·응답으로 변환
3. 서버는 객관식 문항을 집계(무응답 제외, 유효 응답자 수 산출)하고, 주관식 문항은 원문 그대로 정리한다.
4. 정리된 내용을 화면 2로 돌려보내 담당자가 확인한다. (이미지였던 경우 여기서 값을 고칠 수 있다)
5. 담당자가 "결과보고서 생성"을 누르면, 확인된 내용으로 정해진 형식의 결과보고서 Excel 파일을 서버에서 만들어 바로 다운로드해준다.
6. 업로드된 원본 파일과 처리 중 데이터는 서버에 저장하지 않고, 이 과정이 끝나면 남기지 않는다. (PRD 7번 보안 원칙)

## 3. 기술 선택

| 기술 | 왜 필요한가 (쉬운 설명) |
|---|---|
| Next.js | 화면과 서버 코드를 한 프로젝트 안에서 같이 처리하는 기본 틀. (STEP 2에서 이미 설치, Vercel 배포와 그대로 이어짐) |
| TypeScript | 코드를 쓸 때 오타나 실수를 미리 잡아주는 안전장치. |
| Tailwind CSS | 화면 색상·간격 같은 디자인을 빠르게 입히는 도구. |
| exceljs | 서버에서 엑셀 파일을 읽고, 새 엑셀 파일(결과보고서)을 만드는 도구. |
| OpenAI API (Vision) | 캡처 이미지 속 글자·숫자를 AI가 읽어내는 데 쓰는 도구. 이미지를 올렸을 때만 사용. `.env`의 `OPENAI_API_KEY`로 인증. |

**결과보고서 안의 원형 차트**: exceljs에는 차트를 그리는 기능이 따로 없어서, 결과보고서를 만들 때 엑셀 파일 내부에 차트를 직접 끼워 넣는 코드를 추가로 작성한다. 데이터를 외부로 보내지 않고 서버 안에서만 처리한다.

## 4. 참고 — 이번 버전에서 안 하는 것

PRD 6번 비범위 그대로: 여러 교육 일괄 처리, AI 요약·해석, 로그인/계정, 결과보고서 양식 직접 편집, 서버 장기 보관은 이번 버전에 포함하지 않는다.
